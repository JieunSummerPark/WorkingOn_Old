
public class Plinco_08_MoveToLeftRight {

	public static void main(String[] args) {
		
		int boardLength = 9;
		int boardWidth = 9;
		
		String[][] plinkoBoard = new String[boardLength][boardWidth];

		for (int i=0; i<plinkoBoard.length; i++) {
			System.out.print("\t");
			for (int j=0; j<plinkoBoard[i].length; j++) {
				plinkoBoard[i][j] = "XXX";
				System.out.print(plinkoBoard[i][j] + "\t");
			}
			System.out.println("");
		}

		plinkoBoard[1][dropToken]
		if () {
			int ranNum = (int)(Math.random() * 3 - 1);
		}
		int ranNum = (int)(Math.random() * 3 - 1);

	}

}
